# Эксперименты (архив)

Ранние черновики, не входят в основной проект.

| Файл | Описание |
|------|----------|
| `1.py` | Парсинг через requests + BeautifulSoup (не подходит для Kendo Grid) |
| `parse.py` | Ранний Selenium-парсер с numpy и `#grid` |
| `lib.py` | DOM-хелперы для `parse.py` |
| `analiz.py`, `analiz1.py` | Чтение `reclan.csv` (продажи, не прочтения по главам) |

Актуальный код: пакет `author_today/`, точка входа `selenium_stats.py`.
